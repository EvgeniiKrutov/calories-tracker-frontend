import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'calorie-tracker-fe-id-capacitor',
  appName: 'calorie-tracker-fe',
  webDir: 'dist'
};

export default config;
